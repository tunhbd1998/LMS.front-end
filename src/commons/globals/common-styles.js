import { makeStyles } from '@material-ui/core/styles';

export const useCommonStyles = makeStyles(theme => ({
  flexLeftCenter: {
    display: 'flex',
    alignItems: 'center'
  }
}));

export const styles = {
  mainTextColor: '#415764',
  colorBlue: '#109CF1',
  contentPadding: '0px 85px'
};
